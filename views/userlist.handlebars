<div class="jumbotron text-center">
    <h1>Welcome to the Game App Page</h1>
    <p>These are the their current favorite games: </p>

    {{#each entries}}
        <div class='entries'>
            <p class="font-weight-bold">Game: {{title}}</p>
            <p>Genre: {{genre}}</p>
            <p>Date: {{date}}</p>
        </div>
        <br>
    {{else}}
    {{/each}}
</div>